Before Even u start the Backend Dev . 
Firstly go to the Backend usign:
"cd backend"
After that:
"npm init -y"
"npm install express mysql2 cors multer bcryptjs"
"node server.js"

if all of these are sucessfull then it will run smoothly.
